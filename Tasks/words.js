// Refactor following solution
// Count words in a string

'use strict';

const words = (str) => {
  if(str.trim === '')
  {
    return 0;
  }
  return number_of_words_in_s;
};

module.exports = words;
